/**
 * Copy for the hero visualisation. The visual carries the path; these lines say
 * what the layers are, once, so it reads as a life rather than a screensaver.
 */

export const heroCaption =
  'The lattice is the golden angle from the mathematics I trained in. The travelling ring is the front of the harvested invasion my thesis modelled. The swell is the sea the software ships to. Touch the water.';

/** One formula per layer, printed with the piece. The code is the third proof. */
export const heroFormulas = 'r = c√n, θ = n·137.5° · uₜ = D∇²u + ru(1−u) − hu · Σ aᵢ sin(kᵢ·x − ωᵢt)';

/**
 * True and checkable: the parastichy counts of a golden-angle spiral are
 * consecutive Fibonacci numbers. Nobody draws them; they emerge.
 */
export const heroEmergence = 'The spiral arms are not drawn — count them: consecutive Fibonacci numbers.';
