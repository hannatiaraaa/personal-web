/** The gradient tick that marks a claim. Used wherever a list carries weight. */
export function Bullet() {
  return (
    <span
      aria-hidden='true'
      className='from-sky-high to-signal-cyan mt-[0.62em] h-1.5 w-1.5 shrink-0 rounded-full bg-linear-to-br'
    />
  );
}
